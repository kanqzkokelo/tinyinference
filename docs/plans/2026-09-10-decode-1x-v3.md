# Decode parity plan v3 (2026-09-10) — delta on v2

Supersedes: the Downloads mega-plan (stale, 0.58x-era) and
`docs/plans/2026-09-08-decode-1x-roadmap-v2.md` checkpoints 1-12
(history preserved there, not repeated here).

## Objective (unchanged)

```text
geomean(TI / llama.cpp) >= 1.00x decode
each primary model >= 0.90x
no parity/tokenizer/KV/server/CPU regressions
short-ctx and long-ctx reported separately
```

## Where we stand (fleet e998ad3, QKV-fused binary, graph, 5-run medians)

| Model | ctx32 | ctx512 | ctx2048 |
|---|---|---|---|
| qwen2.5-0.5B Q4_0 | 1.01 | 1.05 | 0.94 |
| smollm2-135M Q4_0 | 1.10 | 1.09 | 0.62 |
| qwen3-0.6B Q8_0 | 0.96 | 0.74 | 0.40 |
| llama-3.2-1B Q4_0 | 0.85 | 0.85 | 0.81 |

Graph geomean ~0.84x. Short-ctx decode at/near parity except Llama.
Three distinct problems remain:

1. Long-ctx attention collapse (qwen3 0.40x, smol 0.62x at 2048).
   Measured: qwen3-2048 attn 11.2ms of 16.6ms/tok; Q8 KV cuts it to
   4.0ms, decode 56 -> 93 tok/s. SmolLM2-2048 attn 2.46 -> 1.75ms,
   decode 185 -> 210 tok/s. Root cause is KV traffic, diagnosis closed.
2. Llama flat tax ~0.84x at ALL ctx. Not one broken kernel: larger
   FFN/logits scale + ~1ms unexplained in-situ GEMV deficit + Q6_K head
   (1.7-1.9ms, roofline standalone) + ~0.5ms attn gap at 512.
   Needs several small wins, not one fix.
3. Prefill far behind (llama 17x, qwen3 was 42x before the Q8 GEMM
   fix took 212 -> 814 tok/s). Separate track; out of decode scope but
   drags end-to-end and TTFT cells.

## Killed since v2 (do not reopen without new evidence)

- Q6 4-row/warp (743ba54), Q4 split-K at Q-shape (4581b9e),
  qkv2 2-row + 3xR8 (98e420c), R8 GEMV, fused-qkv-4row, V4-b4 occupancy
  (checkpoint 12), add-norm fusion (NO-GO under graph), attn fast-exp
  duel (+6.7pct total, killed as lever), serial vs split-K Q8 attn
  (51f183d, NO-GO pending 2k data — the ONLY duel with open condition).
- Lesson re-confirmed twice: under graph replay, fusions that only remove
  launches save ~zero. Only traffic cuts (weights/KV) move decode now.

## Work queue (ordered by expected gain)

### W1 — Land Q8-KV hybrid (IN PROGRESS, uncommitted)
Change (`kernels/qwen2_cuda.cu`): Q8 KV default-ON with hybrid
threshold default 256 — FP32 below (short-ctx gates stay bit-identical),
Q8 above (long-ctx gets the 4x KV traffic cut).
Opt-out `TT_Q8_KV=0` / `TT_KV_F16=1`; `TT_Q4_KV` still
overrides; `TT_QKV_THRESH` tunes crossover.

Why hybrid not plain default-ON: plain Q8 default-ON failed the logit
gate 0/7 (median dlogit 0.25-1.0 vs 0.15 gate) while FP32 passed 7/7.
Hybrid keeps the FP32 acceptance baseline for short ctx and buys the
measured long-ctx wins (qwen3-2048 +66pct, smol-2048 +14pct in micros).

Validation order before commit:

1. Build clean (mmcuda nvcc, sm_86).
2. `./scripts/verify.sh m61` — must pass (short ctx stays FP32).
3. Clean greedy-prefix rerun all four primaries, Q8 OFF vs hybrid-ON at
   short ctx: must be identical (prior probe showed a qwen2.5 divergence
   needing a clean rerun to confirm/deny; qwen3/smol/llama matched).
4. Long-ctx decode bench (qwen3-2048, smol-2048, llama-512): expect
   qwen3-2048 large gain, smol-2048 moderate gain, llama-512 neutral.
5. Commit separately from any other change.

### W2 — Fresh full fleet on the W1 binary, then recompute
Do NOT cite the 0.84x geomean for the new binary. Rerun
`bench/bench_llm.py` fleet (graph+eager, 5-run medians), refresh
`bench/scoreboard_decode.csv` + `data/bench/results_scoreboard.md`,
then re-rank the queue. Expected shape after W1: qwen3-2048 ~0.6-0.7x,
smol-2048 ~0.7x, rest unchanged.

### W3 — Qwen3/SmolLM2 residual long-ctx gap
Candidates: 2k-conditioned attention duel reopen (serial-vs-split at
Llama 32h/8kv/HD64 ctx512 was short-ctx; 2k data still open per 51f183d),
attention occupancy at 2k KV lengths, then prefill-side Q8 routing
(qwen3-512 decode may inherit the slow Q8 prefill-path kernel).

### W4 — Llama constant tax (final grind, several small wins)
Ordered by quantified size: (a) ~1ms unexplained in-situ GEMV deficit
(85pct SM clock in-pipe + queue bubbles; not occupancy — killed in
checkpoint 12); (b) Q6_K head tune (-0.4ms est; v3 sweep still open);
(c) attention kernel -0.4ms est; (d) launch-bubble trim: rope q/k 2x/layer
(32 nodes) + k_add residuals (32 nodes) + gumbel/penalty passes when
greedy — graph census 216->200 after scatter-skip, these are next;
(e) `TT_NO_FUSE` qkv gating is model-dependent (Q8 fused +1.5pct,
Llama-Q4 unfused +1.5pct short ctx) — own ctx512 experiment before
touching committed gating.

### W5 — Prefill track (separate, does not block decode claim)
Q8 prefill GEMM landed (6.7-7.8x gap -> 1.76x byte floor, bit-exact).
Still 10-17x behind: suspect token-at-a-time prefill routing where a
batched GEMM/cuBLAS path belongs. Profile `TT_DISPATCH=1` on a
509-tok prompt first. Never let prefill kernels leak into decode decisions.

### W6 — Methodology hardening (before ANY parity claim)
From the v5 TRUST-WITH-CAVEATS verdict: 3 warmups (oracle cold-start
ramp visible), per-row timestamps, store oracle prompt_n/predicted_n,
log nvidia-smi clocks/power + AC state, cooldown between models.
Some landed (ccb1918 power snapshot); per-row stamps and oracle token
counts still open.

## Rules of engagement

- One change per commit; fleet numbers always cite the binary commit.
- Same-process A/B + gate before committing any kernel change.
- No-go results commit the tool + one-line verdict (practice).
- Do NOT touch until Llama clears 0.90x default-safe: Q4-KV (greedy
  divergence), speculative decoding, LM-head requant, tokenizer/sampler
  rewrites, CPU GEMM.
- Probe clutter (`tools/micro_*`, `tools/print_ids.c`,
  `tools/requant_head.c`, `=5899`, `vpn-rotate.sh`,
  `AUDIT.md`) stays untracked until owner decides;
  never bundle into feature commits.

## Definition of done

```text
fresh-fleet graph geomean >= 1.00x
AND every primary cell >= 0.90x (short and long reported separately)
AND ./scripts/verify.sh all green on the claiming commit
AND W6 hardening items closed so the claim is auditable
```

## W1 result 2026-09-10 (commit ab55f89): LANDED

Same-binary 2048-ctx A/B, 5-run medians, ours-only: qwen3 60.5 to 125.1
tok/s (+107pct), smol 270.4 to 308.9 (+14pct), llama 112.3 to 115.0 (+2pct),
qwen2.5 244.9 to 235.0 (-4pct, tiny-KV dequant tax, accepted). m61 gate 7/7.
Greedy probes hybrid == FP32-eager on all 4 primaries (prior qwen2.5 scare was
timing lines, not text). Short-ctx qwen2.5-32 unchanged. Costs in commit msg:
graph disabled under hybrid; qwen3-2k Q8 greedy EOS early on filler-soup only.
Machine read 75W not 60W: W2 must confirm power first. Follow-ups (pre-existing):
qwen3+smol graph-vs-eager greedy DIVERGE on FP32 path, M7 needs per-model
re-scoping; qwen3-2048 cell may mix gen lengths; qwen2.5 -4pct vs 0.90 floor.

## Status 2026-09-10 eve: W2 fleet v6 DIED at launch, new correctness bug found

- W2 fleet v6 (70W filename) died instantly: empty log, no output files.
  MUST relaunch supervised before any parity claim. Machine idle.
- NEW BUG (queued as W2b, top priority): graph capture in
  kernels/qwen2_cuda.cu (~line 6650) has no Q8_0 embed branch; gate admits
  Q8_0 and warmup runs k_embed_q8_0_dyn, but capture falls into else (Q6_K).
  token_embd.weight is Q8_0 in qwen3-0.6b AND smollm2-135M (Q4_0 qwen2.5,
  Q6_K llama). Explains qwen3+smol graph-vs-eager greedy divergence.
  Fix = mirror warmup Q8_0 branch in capture, probe, gate, commit.
- Power enforced reads ~70W; v6 filename accordingly.
- Queue: W2b fix -> relaunch W2 supervised -> W3/W4 re-rank -> W5 -> W6.

## Status 2026-09-10 night

- W2b LANDED (d06f3ee): Q8_0 embed branch added to graph capture.
  Greedy graph-vs-eager now IDENTICAL on all 4 primaries (FP32 KV path).
  Root cause of the old qwen3+smol graph divergence note above -- that note
  is now closed. Speed unaffected (correctness-only fix).
- W2 fleet v6 relaunched supervised in tmux session fleet (commit d06f3ee),
  outputs to results_scoreboard_v6.jsonl + scoreboard_fleet_70w_v6. Lesson:
  background nohup procs are reaped when the agent shell ends; tmux survives.
  Power reads 70-75W (filename 70w; same-session ratios valid, not
  cross-comparable to 60W fleets).

## W2 result 2026-09-10 (commit d06f3ee): fleet v6 COMPLETE, 0.910x overall

- 24/24 cells, 5-run medians. Graph geomean 0.898x, eager 0.922x.
- Graph cells vs 0.90 floor: qwen2.5 1.101/1.058/0.925 PASS; qwen3
  0.923/0.853/0.901 (512 FAIL); llama 0.913/0.851/0.733 (512+2048 FAIL);
  smol 0.921/0.886/0.773 (512+2048 FAIL). Target needs every cell >=0.90.
- Anomalies: (a) smol graph SLOWER than eager at 32 (0.921 vs 1.118) and
  512 (0.886 vs 0.984) but FASTER at 2048 (0.773 vs 0.714) -- graph
  inversion, top W3 suspect (split-S baked at capture ctx? fixed cost?).
  (b) qwen3-2048 gen counts 5/3 (Q8 greedy EOS early, known) -- rates
  ~0.90 but tiny samples, noisy. (c) llama-2048 0.73 the single worst
  cell -- W4 main course. Prefill still 5-10x behind (W5, separate).
- Machine rebooted after fleet (up 8min at harvest); /tmp wiped, tmux gone.
  Artifacts survived: results_scoreboard_v6.jsonl + fleet_70w_v6 md/csv.
- Re-ranked queue: W3 = smol graph inversion + qwen3-512 + smol-2048;
  W4 = llama-512/2048; W5 prefill unchanged; W6 clocks patch landed after
  fleet started so v6 rows lack clocks_* (next fleet will have them).

## W4 profile 2026-09-10: llama-2048 decode breakdown (TT_PROFILE, eager med ms)

- Per-step 7.80ms (117 tok/s) vs oracle 6.0ms (166 tok/s): gap ~1.8ms.
- attn 2.065 (26pct) / ffn-gateup 1.860 (24pct) / logits-gemv 1.638 (21pct)
  / ffn-down 1.040 (13pct) / qkv 0.542 / o-proj 0.331. Long tail is
  vocab=128k head + GQA-32/8-HD64 attn at 2k ctx, matching W4 order:
  (a) in-situ GEMV deficit, (b) Q6_K head tune, (c) attention kernel.
- W3 smol-32 graph inversion NOT reproduced same-binary (475 vs 476 tok/s
  after clock ramp; early pairs differ = ramp, not graph tax). Fleet gap
  likely power-limit wobble between cells (60<->75W observed). Next fleet
  has per-row clocks_* to normalize; do not chase as kernel bug.

## Status 2026-09-11: W6 clocks logging landed, in-pipe clock experiment open

- W6 LANDED PARTIAL (32c61a9): bench logs per-row clocks_* (SM/gr/mem,
  power draw, temp, throttle reasons) + power.limit into each jsonl row.
  v6 rows lack it (patch landed after fleet start); next fleet will have it.
- In-pipe SM clocks under llama-2048 load (5-run medians were 7.80ms ours
  vs 6.0ms oracle, gap ~1.8ms): OURS sampled in-pipe = 1815MHz @ ~74W.
  85pct standalone + in-pipe agree: chip runs 1815-1950, cannot be pushed.
  Oracle in-pipe sample was launched but output truncated -- RETRY FIRST.
  If oracle also ~1815MHz, clock theory is DEAD for the gap and W4 is pure
  kernel work: (a) in-situ GEMV, (b) Q6_K head tune
  (kernels/gemv_typed.cu k_gemv_q6_K_v2), (c) attn k_fa2_q8_split.
- Next: oracle in-pipe clocks -> W4 kernels (llama-512/2048) + W3
  (qwen3-512, smol-2048) -> next fleet with clocks_* rows -> W5 prefill.

## Status 2026-09-11: oracle in-pipe clocks DONE, clock theory dead

- Oracle llama-2048 greedy decode steady-state in-pipe: 1500MHz SM @
  ~18W (3 consistent samples mid-run, --no-display-prompt, -n 1024).
  Ours prefill: ~1852MHz @ 72-75W. Ours greedy decode window too short
  to sample cleanly (greedy EOS at ~53 toks on filler-soup, 0.43s).
- Verdict: oracle is FASTER (6.0 vs 7.8ms/step) at LOWER clocks. Clock
  theory dead in both directions -- gap is pure kernel efficiency (W4).
  No DVFS/p-state excuse; do not revisit without new evidence.
- Sampling caveat: --temp 0.8 halves ours decode (63 vs 124 tok/s) --
  sampler overhead dominates; never use sampled mode for kernel A/B.
- Method notes: bg procs reaped on exec-session end (use foreground +
  parallel sampler sessions); clocks.sm query sticks at 210MHz, use
  clocks.gr; always --no-display-prompt (77MB spinner logs otherwise).
- Next: W4 same-process A/B duels (in-situ GEMV deficit, Q6_K head
  k_gemv_q6_K_v2, attn k_fa2_q8_split), then W3, then fleet with clocks_*.

## Status 2026-09-11: attn 2k duel closed, split stays (8cec425)

- 51f183d open condition now has 2k data: llama-2k serial 1412us vs
  splitS32 119us (11.8x) -- split wins overwhelmingly, serial buried.
  Matches TT_PROFILE (16x120us=1.9ms of 2.065ms attn): attn in-situ ==
  standalone, no launch-environment deficit here. Fault is IN the kernel.
- S-sweep llama-2k (split+combine, L2-flushed): S4 175 / S8 124 /
  S16 115 / S32 120us. S16 best but only ~1pct/step -- below commit
  threshold with numerics risk. NO-GO on S change; curve is flat for
  S>=8, kernel is overhead-dominated (~19 usefulGB/s vs 135 GEMV roof).
- Real attn fix needs structural work (combine fusion / bigger tiles),
  queued as W4c2. Stack math for llama-2048 (+23pct needed): attn half
  =1ms, Q6 head tune ~0.2-0.3, rest from FFN stack -- grind, no single shot.
- Next: Q6_K v3-idea duel at head shape (wider loads / scales hoist),
  then W4c2 attn rework, W3, fleet with clocks_*.

## Status 2026-09-11: Q6 head v3 NO-GO, v2 is the ceiling (3fb9f84)

- v3 = v2 + split accumulators (even/odd sb halves ILP). Result: 0.96x
  vs v2, maxabs 9.8e-04 (same order as v1-vs-v2 dequant noise, benign).
  Extra registers cost occupancy; ILP does not help = kernel is
  bandwidth-bound, not latency-bound. v4 re-confirmed 0.91x.
- Tried for Q6_K head: scalar(0.2x) < v4-4row(0.9x) < v3-splitacc(0.96x)
  < v2-2row. Direction CLOSED. Head stays ~1.64ms in llama-2048 budget.
- Side lesson: float4-x idea died on paper -- scalar x loads are strided
  (lane/lane+32/64/96) and already exactly coalesced; vectorizing would
  need a shfl transpose for zero gain. Do not reopen Q6 head without
  new evidence (e.g. different quant of head weights).
- Next: W4c2 structural attn rework (only remaining >0.5ms lever), then
  W3 (qwen3-512, smol-2048), fleet with clocks_*.

## Status 2026-09-11: W4c2 expf-dedup NO-GO standalone, reverted

- Root-cause candidate was wrong in magnitude, right in fact: k_fa2_q8_split
  did 2x expf per token per lane (32 lanes redundant). Fix lane-0+shfl was
  bit-exact (duel maxdiff=0.00000 all 5 shapes incl llama-2k) but splitS32
  timed 123us vs 119us pre-patch = noise. Saving is ~0.3us per BLOCK
  (32 toks x SFU contention), not ~1ms per step -- estimate wrongly
  multiplied per-step instead of per-block. Reverted, tree clean.
- Lesson: kernel is overhead-dominated (18 usefulGB/s vs 135 roof), so
  per-FLOP trims cannot move it. Only structural cuts count: fewer blocks,
  fewer passes over tokens, combine fusion, or FA-style tile restructure.
- Same redundancy exists in k_fa2_q4_split / k_fa2_fp32_split / serials --
  deliberately NOT fixed (zero-gain, adds risk). Do not reopen.
- Next W4c2: read k_fa2_q8_split fully, test S64/S128 sweep point (only
  S<=32 swept), cost combine kernel separately, then biggest structural cut.

## Status 2026-09-11: W4c2 sweep extended + split/combine decomposed

- S sweep llama-2k now 1/8/16/32/64: 532 / 125 / 116 / 120 / 121 us.
  S=1 (8 blocks x 2049 toks) = 532us validates the per-token chain model
  (~470c: dot + warp_sum shfls + 2x expf + acc). S>=8 flat ~120us =
  traffic-bound; no S value beats it. S16 stays default, do not revisit.
- Decomposition via split-only/combine-only wrappers: split S32 = 105us
  for 2.36MB useful (22GB/s achieved), combine S32 = 25us cold-L2
  (~15us in-situ with hot partials). Regs split 48 / combine 35:
  occupancy is NOT the cap. All shapes sit 13-30 usefulGB/s.
- Verdict: attn is DRAM-efficiency-bound. Prime suspect is the 34B
  BlockQ8_0 KV struct (misaligned loads) on 544B strides (row-hit
  killer). Next: aligned KV-cache-only struct, float scale, 36B, Q8
  cache path only. Weights keep GGUF 34B (separate struct, on-disk).
- Untouched by design: Q4/FP32/FP16 cache paths, weight layouts,
  combine (15us in-situ is not the lever), S default.

## Status 2026-09-11: W4c2 aligned-KV NO-GAIN, misalignment theory dead

- Built BlockQ8KV (36B: half scale + int16 pad + 32B qs), Q8-cache-only;
  weights keep GGUF 34B. Touched 3 writers (scatter x2, backfill) + 3
  readers (serial byte-math, split, prefill) + casts + engine decls.
  Duel on aligned KV llama-2k: 117.9us vs 119.8us pre-patch = noise.
  Bit-exact everywhere (maxdiff=0.00000 all shapes, also proves the
  serial byte-math edit correct). UNCOMMITTED in tree (kernels +
  duel tool); lean REVERT unless a follow-up needs the layout.
- 34B-struct misalignment is NOT the DRAM-efficiency cap. Suspect now
  shifts to access pattern itself (544B strides = row-hit killer
  regardless of 2B alignment) or per-token chain latency.
- Next: hot-L2 duel (argv-gated flush, build in flight) to split the
  two: hot<<cold = DRAM-pattern-bound (attack strides); hot~=cold =
  chain/latency-bound (attack per-token work). Also ctx510 (<L2) point.

## Status 2026-09-11: hot-L2 verdict DONE, attn is chain-bound not DRAM-bound

- Duel argv-gated flush (build7, bit-exact maxdiff=0.00000 all shapes):
  llama-2k splitS32 hot 116.7us vs cold 119.8us; ctx510 splitS8 hot
  51.2us vs cold 43.0us (hot slower = noise). Split does NOT speed up
  with hot L2, while serial does (226 vs 352us) and combine halves
  (12.3 vs 24.6us). KV traffic is overlapped/hidden; the cap is the
  per-token dependency chain (~470c: dot + warp_sum shfls + 2x expf +
  acc) + fixed per-block overheads. S>=8 flat corroborates.
- Consequence: stride/layout work is OFF the table (aligned-KV already
  proved it: zero gain). Only cuts to total chain work or block
  overhead count: single-pass softmax already assumed -- READ
  k_fa2_q8_split fully, count passes over tokens and shfls per token,
  then structural cut (fewer passes / wider per-thread tiles /
  int4 qs loads). Combine fusion capped at ~15us, not the lever.
- Verdict: W4c2 = read kernel + one structural duel. Aligned-KV tree
  changes stay UNCOMMITTED until then (revert leans yes: +6pct cache
  bytes, zero gain, but harmless while dueling).

## Status 2026-09-11: unfused-attn duel BREAKTHROUGH (correctness bug open)

- Unfused decode attn (QK-gemv + row-softmax + PV-gemv, pure streaming,
  no warp-sync chain) vs fused k_fa2_q8_split, same-process A-B.
  v1 naive per-thread: CORRECT (maxdiff=0) but 0.11x (64x V-reread).
  v2 block-per-kv-chunk + smem staging + G-way reuse: bit-exact, 0.68x
  (qk~99 sm~9 pv~104us at llama-2k).
- TT2 64->256 + qs-first-36B + u32 vector loads: qk 85->26us.
  1.38x overall at llama-2k (unf 87 vs split 120us), 1.82x ctx510,
  1.25x qwen3-2k. BUT maxdiff=0.40-0.63: values wrong, timing real.
  u32-aliasing cleanup did NOT fix; scalar-byte version was exact.
  Suspects: u32 coop-copy indexing, TT2=256 smem sizing, dual-buf pack.
- NOTE: engine cache layout changed by aligned-KV exp (BlockQ8KV 36B);
  duel mirrors it via BlockQ8E. Aligned-KV tree still UNCOMMITTED.
- Next: isolate bug (revert coop copy to scalar first), attack pv
  (~103us, suspect prow traffic), engine-ize on duel win with
  verify.sh m61 + greedy graph-vs-eager identical + same-process A/B.

## Status 2026-09-11: unfused bug FIXED, duel GREEN bit-exact

- Root cause was host-side, not kernel: dV2 memcpy took token-major
  hkv2 while k2_pv indexes kv-major (dK2 correctly took hkv2n).
  One-line fix, no kernel change. u32 coop copy + TT2=256 + dual-buf
  all exonerated.
- Hot-L2 same-process medians, maxdiff=0.00000 all shapes:
  ctx510 1.58x (31.7 vs 50.0us), llama-2k 1.39x (107.5 vs 149.4us),
  qwen3-2k 1.36x (152.6 vs 207.9us). Split absolutes wobble run to
  run; trust back-to-back ratios only.
- pv is now the biggest chunk (llama-2k 68.6 of 104.4us, qwen3-2k
  128.0 of 178.2us). Suspects: single acc chain over 2048 iters,
  half-idle threads in compute (GD2=128 of 256), 3 DRAM passes over
  scores. Next: pv ILP + full-block-per-kv duel variants, then
  engine-ize with verify.sh m61 + greedy graph-vs-eager identical.
